{
  "methods" : {
    "Members.Add" : {
      "description":  "Add a new person as member",
      "params" : [
        {
          "$ref": "#types/Person",
          "default": -1,
          "name": "playerid"
        }
      ],
      "returns": {
        "type": "string"
      }
    }
  },
  "notifications" : {

  },
  "types": {
    "Person": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "name": {
          "$ref": "#/definitions/Name"
        },
        "dateOfBirth": {
          "type": "string",
          "format": "date"
        },
        "address": {
          "$ref": "#/definitions/Address"
        }
      },
      "required": [
        "address",
        "dateOfBirth",
        "name"
      ],
      "title": "Person"
    },
    "Address": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "street": {
          "type": "string"
        },
        "zip": {
          "type": "string",
          "format": "integer"
        },
        "city": {
          "type": "string"
        },
        "state": {
          "type": "string"
        }
      },
      "required": [
        "city",
        "state",
        "street",
        "zip"
      ],
      "title": "Address"
    },
    "Name": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "first": {
          "type": "string"
        },
        "last": {
          "type": "string"
        }
      },
      "required": [
        "first",
        "last"
      ],
      "title": "Name"
    }
  }
}